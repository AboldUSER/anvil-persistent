services:
  anvil:
    build: .
    container_name: anvil
    ports:
      - "8545:8545"
    volumes:
      - anvil-data:/data
    environment:
      STATE_INTERVAL: 1800
    stop_signal: SIGINT
    stop_grace_period: 30s
    restart: unless-stopped

volumes:
  anvil-data:
